{{#each posts}}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2><a href="/blog/{{id}}">{{title}}</a></h2>
  </div>
  <div class="col-md-7">
    <p>{{description}}</p>
  </div>
</div>
{{/each}}
